import 'package:flutter/material.dart';

class LoginTheme {
  // PADDINGS
  static const usernameScrollPadding = EdgeInsets.all(50);
  static const fieldPrefixIconPadding = EdgeInsets.symmetric(vertical: 20);
  static const passwordScrollPadding = EdgeInsets.all(100);
  static const eyeIconPadding = EdgeInsets.all(8);
  static const bottomOptionRowPadding = EdgeInsets.only(bottom: 25);

  // SIZES
  static const double fieldPrefixIconSize = 20;
  static const double eyeIconSize = 18;
  static const double minPasswordLength = 8;
}
